# Security Policy

Do not file public issues for vulnerabilities that could expose repository contents, credentials, or allow unintended Git execution. Until a dedicated security contact is published, report responsibly to the maintainers through the repository's private reporting channel.

Gitronimo will not collect telemetry by default. Credentials remain the responsibility of the installed Git credential helper and SSH configuration during the MVP.

